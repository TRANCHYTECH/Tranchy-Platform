import { TextStyle, ViewStyle } from "react-native"

export const $rightTextGrow: TextStyle = {
  flexGrow: 1,
  textAlign: "right",
}

export const $row: ViewStyle = {
  flex: 1,
  flexDirection: "row",
}
